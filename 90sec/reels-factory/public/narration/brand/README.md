# Brand welcome audio (ONE-TIME)

`welcome-daily-dose-hi.wav` / `welcome-daily-dose-hi-mixed.wav`

- Locked line: `नमस्ते… AstroAvatar की डेली डोज़ में आपका स्वागत है।`
- Voice: instruct `male, middle-aged, indian accent, moderate pitch` — speed `0.95`, bass `8 dB`, seed `42`, **no `--duration`**, **no ref clone**
- Single pass only
- **Do not regenerate per episode.** Regenerate only if the locked welcome text **or** the locked voice recipe changes.

Story episodes use separate files under `public/narration/<episode>-hi.wav` (prefer **1 single pass** per episode).
